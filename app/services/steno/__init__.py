"""Steno Portfolio Mirror — Real Vision PDF ingestion + Claude-vision extraction.

Ported from sibling `steno-bot` repo with a new portfolio-focused analyzer that
extracts target weights per ticker instead of macro-only insights.
"""
